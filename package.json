{
  "name": "excepio",
  "version": "0.0.1",
  "private": true,
  "description": "Sistema de Registro y Consumo de Excepciones",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d postgres",
    "db:regenerate": "pnpm --filter @excepio/api exec prisma migrate reset --force && pnpm --filter @excepio/api exec prisma db seed"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
